#include "Delay.h"
#include "neixie.h"
#include <REGX52.H>
void main(){
    while(1){
     nixie(1,2);
     //Delay(5);
    nixie(2,0);
     //Delay(5);
    nixie(3,2);
     nixie(4,3);
     //Delay(5);
}
}
